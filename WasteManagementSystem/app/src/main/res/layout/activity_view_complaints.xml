<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:id="@+id/drawercomplaint"
    android:layout_height="match_parent"
    tools:context=".ViewComplaints">
    <com.google.android.material.navigation.NavigationView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/navmenucomplaint"
        android:layout_gravity="start"
        />
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <androidx.appcompat.widget.Toolbar
            android:layout_width="match_parent"

            android:layout_height="wrap_content"
            android:id="@+id/toolbarcomplaint"
            app:title="Admin DashBoard"
            app:titleTextColor="@color/black"
            android:background="@color/colornav"/>

        <ImageView
            android:layout_width="250dp"
            android:layout_below="@id/toolbarcomplaint"
            android:layout_marginTop="20dp"
            android:id="@+id/imgView"
            android:layout_height="180dp"
            android:layout_centerHorizontal="true"
            android:src="@drawable/compl" />
    <ListView
        android:id="@+id/myListViewversion2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="20dp"
        android:layout_below="@id/imgView"
        android:dividerHeight="2dp"
        android:divider="@color/black"
        tools:ignore="MissingConstraints"
        tools:layout_editor_absoluteX="0dp" />

    </RelativeLayout>
</androidx.drawerlayout.widget.DrawerLayout>