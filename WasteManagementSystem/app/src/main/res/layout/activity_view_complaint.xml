<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerForaSPECIFC"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ViewComplaint">
    <com.google.android.material.navigation.NavigationView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/navmenucomplaintuSER"
        android:layout_gravity="start"
        />
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <androidx.appcompat.widget.Toolbar
            android:layout_width="match_parent"

            android:layout_height="wrap_content"
            android:id="@+id/toolbarcomplaintuSER"
            app:title="Admin DashBoard"
            app:titleTextColor="@color/black"
            android:background="@color/colornav"/>

        <ImageView
            android:layout_width="250dp"
            android:layout_below="@id/toolbarcomplaintuSER"
            android:layout_marginTop="20dp"
            android:id="@+id/imgViewuser"
            android:layout_height="180dp"
            android:layout_centerHorizontal="true"
            android:src="@drawable/complaint" />
        <ListView
            android:id="@+id/myListViewUserComplaint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginTop="20dp"
            android:layout_below="@id/imgViewuser"
            android:dividerHeight="2dp"
            android:divider="@color/black"
            tools:ignore="MissingConstraints"
            tools:layout_editor_absoluteX="0dp" />

    </RelativeLayout>
</androidx.drawerlayout.widget.DrawerLayout>